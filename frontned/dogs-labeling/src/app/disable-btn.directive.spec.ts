import { DisableBtnDirective } from './disable-btn.directive';

describe('DisableBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableBtnDirective();
    expect(directive).toBeTruthy();
  });
});
